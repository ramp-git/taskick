# taskick decisions

このファイルは、今後の AI 継続利用時に設計判断の理由を失わないための意思決定記録です。

- 対象: これまでの会話で明確に決定・実装された事項
- 除外: 未決事項、仮案のみの議論、一般論、雑談
- 注意: 理由が会話上十分に明示されていないものは「理由不足」として記載します

## 2026-06-02 / アプリ全体方針

### 決定事項

- `taskick` は、バックエンドを持たないクライアントサイドのタスク管理 Web アプリとして実装する。
- データ保存先は `localStorage` とする。
- タスクの基本データ構造は以下を前提にする。

```ts
type Task = {
  id: string;
  type: string;
  title: string;
  completed: boolean;
  createdAt: string;
  updatedAt: string;
};
```

### 理由

- 初期要件で保存先として `localStorage` が明示されたため。
- 初期要件で上記 `Task` 型が提示されたため。
- バックエンドや DB を追加しないことで、実装・運用・保守コストを抑える方針に合うため。

### 却下案

- バックエンド API / DB を追加する案は採用しない。
  - 理由: 要件にないため。過剰設計を避ける方針に反するため。
- 期限日など、提示された `Task` 型にないフィールドを追加する案は採用しない。
  - 理由: 「推測で補完しない」という指示があり、期限フィールドは未確認のため。

### 注意点

- `localStorage` はブラウザ内保存のため、端末・ブラウザをまたいだ同期はしない。
- データ構造に期限フィールドがないため、期限による分類・通知・期限切れ判定は実装しない。

### 関連制約

- ユーザー指示: 推測で補完しない。
- ユーザー指示: 過剰設計を避ける。
- ユーザー指示: 保守コストを考慮する。

## 2026-06-02 / React 採用と読み込み方式

### 決定事項

- 表示部分は React で描画する。
- React / ReactDOM は CDN から読み込む。
- JSX / ビルドツールを前提にせず、`React.createElement` ベースで実装する。

### 理由

- ユーザーから「表示部分はReactにできるかな？」という要望があり、React UI へ変更したため。
- npm registry へのアクセスで `react` / `vite` 取得が 403 となったため、npm パッケージ追加を前提にしない構成を選んだ。
- JSX 変換や bundler を入れないことで、依存関係とセットアップコストを抑えるため。

### 却下案

- npm から React / Vite などを取得して構成する案は採用しない。
  - 理由: この環境では npm registry へのアクセスが 403 になったため。
- JSX 前提の React 実装は採用しない。
  - 理由: JSX 変換環境が必要になり、依存追加・ビルド構成が増えるため。
- 以前の手書き DOM 文字列描画は採用しない。
  - 理由: React 表示への変更要望が明確に出たため。

### 注意点

- CDN にアクセスできない環境では React が読み込めない可能性がある。
- React が読み込めない場合に真っ白にならないよう、React 未読込時の fallback 表示を置く。
- CDN 到達性は環境依存であり、このコンテナでは unpkg への確認が 403 になったため未確認。

### 関連制約

- npm registry へのアクセス制限があった。
- 外部 CDN へのアクセス可否は利用環境依存。
- 保守性・コスト観点から、依存追加は最小限にする。

## 2026-06-02 / 起動・ビルド方針

### 決定事項

- `npm run start` は `python3 -m http.server 5173` で静的配信する。
- `npm run build` は `dist/` に `index.html` と `src/` 配下の静的ファイルをコピーする簡易ビルドとする。
- `dist/` は `.gitignore` 対象にする。

### 理由

- 依存パッケージを追加せずにローカル起動できるため。
- bundler を使わない構成のため、成果物生成は静的ファイルコピーで足りるため。
- 生成物を Git 管理しないことで、リポジトリを軽く保てるため。

### 却下案

- Vite などの開発サーバー / bundler を使う案は採用しない。
  - 理由: npm registry へのアクセスで 403 が発生し、依存取得が不安定だったため。

### 注意点

- `build` は最適化や minify を行わない。
- CDN React を参照するため、`dist/` 配布後も CDN 到達性が必要。

### 関連制約

- npm registry 制限。
- 過剰設計を避ける方針。
- 小規模アプリとしての保守コスト優先。

## 2026-06-02 / ページ構成・ナビゲーション

### 決定事項

- トップページは「未完了タスク」ページとする。
- トップページには未完了タスクのみ表示する。
- 完了タスクは別の「完了タスク」ページに表示する。
- 設定は別の「設定」ページに表示する。
- ページ下に固定メニューを表示し、以下へのリンクを置く。
  - 未完了タスク
  - 完了タスク
  - 設定
- タスク追加ボタンはトップページ下部に表示する。

### 理由

- ユーザーから以下の明確な指定があったため。
  - トップページには未完了タスクだけを表示する。
  - ページ下にメニューを表示する。
  - 完了タスク / 設定へのリンクはページ下メニューに表示する。
  - タスク追加はトップページ下にボタン表示する。

### 却下案

- サイドバー型メニューは採用しない。
  - 理由: ページ下メニューへ変更する指定があったため。
- トップページで未完了 / 完了タスクを両方表示する案は採用しない。
  - 理由: トップページは未完了タスクのみという指定があったため。
- タスク追加ボタンをページ上部に置く案は採用しない。
  - 理由: トップページ下に表示する指定があったため。

### 注意点

- 下部メニューは固定表示のため、コンテンツ下部が隠れないよう `shell` 側に下余白を持たせる。
- 「リンク」として下部メニューを扱うため、`a` 要素を使い、クリック時に React state を切り替える。

### 関連制約

- モバイル利用を想定し、ページ下メニューのほうが操作しやすいという判断。
  - ただし、この理由は会話内で明示されたものではないため理由不足。

## 2026-06-02 / トップページ表示

### 決定事項

- トップページのタイトルは「未完了タスク」とする。
- トップページでは「未完了タスク 1件」のような件数付きタイトルを表示しない。
- トップページのリスト上部に重複する「未完了タスク」ヘッダを表示しない。
- トップページの「今日」「期限切れ」「期限なし」カードは表示しない。

### 理由

- ユーザーから以下の明確な指定があったため。
  - 「トップ」ページのタイトル部分は「未完了タスク」に変更。
  - 「未完了タスク1件」の表示は不要。
  - 「未完了タスク」ヘッダは不要。
  - 「今日」「期限切れ」「期限なし」の表示は削除。

### 却下案

- 期限分類カードを表示する案は採用しない。
  - 理由: 明確に削除指定があったため。
- 件数付き見出しを表示する案は採用しない。
  - 理由: 不要指定があったため。
- ページタイトルと一覧セクションヘッダを両方表示する案は採用しない。
  - 理由: 重複ヘッダが不要指定されたため。

### 注意点

- 件数は UI 見出しには出さない。
- 空状態メッセージは維持する。

### 関連制約

- 画面をシンプルにする方針。

## 2026-06-02 / 完了タスクページ表示

### 決定事項

- 完了タスクページのタイトルは「完了タスク」とする。
- 「完了タスク 1件」のような件数付きタイトルを表示しない。
- リスト上部に重複する「完了タスク」ヘッダを表示しない。
- 「完了データ全削除」ボタンを完了タスクページ下部に表示する。
- 「完了データ全削除」ボタンの位置とサイズは、トップページ下部の「タスク追加」ボタンと同じにする。
- 「完了データ全削除」ボタンの色は既存の danger 色を維持する。
- 完了タスクが 0 件の場合、「完了データ全削除」ボタンは disabled にする。

### 理由

- ユーザーから以下の明確な指定があったため。
  - 「完了タスク1件」の表示は不要。
  - 「完了タスク」ヘッダは不要。
  - 「完了データ全削除」ボタンを追加。
  - ボタンはトップページの「タスク追加」ボタンの位置とサイズ。
  - 色は今のままで OK。
- 完了タスクが 0 件のときに削除操作を無効化することで、不要な確認ダイアログを避けるため。

### 却下案

- 完了データ全削除ボタンをページ上部に置く案は採用しない。
  - 理由: トップページの追加ボタンと同じ位置・サイズにする指定があったため。
- 完了タスク件数をタイトルに含める案は採用しない。
  - 理由: 不要指定があったため。
- 色を変更する案は採用しない。
  - 理由: 今の色で OK という指定があったため。

### 注意点

- 全データ削除とは異なり、完了タスクのみを削除する。
- 実行前に確認ダイアログを出す。

### 関連制約

- 誤削除を避けるため、確認ダイアログを維持する。

## 2026-06-02 / タスク表示順

### 決定事項

- 未完了タスク・完了タスクともに、タスク登録日 `createdAt` の降順で表示する。
- 新しく登録されたタスクほど上に表示する。
- 手動並び替え UI は表示しない。

### 理由

- ユーザーから「表示順はタスク登録日降順。新しく登録された方が上」と明確に指定があったため。
- 登録日降順が固定仕様になったため、手動並び替えと併用すると仕様が曖昧になるため。

### 却下案

- 手動並び替えボタンによる順序変更は採用しない。
  - 理由: 登録日降順が指定されたため。
- 完了状態の更新日時 `updatedAt` 順で表示する案は採用しない。
  - 理由: 指定は登録日 `createdAt` 降順であり、更新日順ではないため。

### 注意点

- 編集や完了切替で `updatedAt` が変わっても、表示順は `createdAt` 基準のまま。

### 関連制約

- `createdAt` が不正な既存データの扱いは未確認。

## 2026-06-02 / タスク項目レイアウト

### 決定事項

- タスク項目は 2 行表示にする。
- 1 行目に以下を横一列で表示する。
  - チェックボックス
  - 種類
  - 編集ボタン
  - 削除ボタン
- 2 行目にタスク名を表示する。
- 編集ボタンは鉛筆アイコン表示にする。
- 削除ボタンは赤い `×` アイコン表示にする。

### 理由

- ユーザーから明確なレイアウト指定があったため。
- タスク名を 2 行目に分けることで、操作要素とタスク本文の視認性を分離できるため。
  - この視認性の理由は実装上の判断であり、会話内で明示されたものではないため理由不足。

### 却下案

- 1 行にチェックボックス・種類・タスク名・編集・削除をすべて並べる案は採用しない。
  - 理由: 2 行表示指定があったため。
- 編集 / 削除をテキストボタンのままにする案は採用しない。
  - 理由: 鉛筆アイコン / 赤×アイコン指定があったため。

### 注意点

- アイコンボタンには `aria-label` と `title` を付け、意味が分かるようにする。
- 削除ボタンは danger 配色を使う。

### 関連制約

- モバイル幅でも 1 行目の操作要素が横並びを保つようにする。

## 2026-06-02 / タイトルヘッダー

### 決定事項

- アプリ全体の「タスク管理」タイトル部分は、説明文を省いたコンパクトなヘッダーにする。
- `T` のマークと `taskick` / `タスク管理` の短いタイトル構成にする。
- 角丸・グラデーション・小さめのタイトルで、よりシンプルかつ現代的な見た目にする。

### 理由

- ユーザーから「もっとシンプルかつスマートに今っぽいデザイン」と明確な要望があったため。
- 長い説明文をヘッダーから外すことで、タイトル領域をコンパクトにできるため。

### 却下案

- 説明文付きの大きいヘッダーは採用しない。
  - 理由: シンプルでスマートな見た目への変更要望があったため。

### 注意点

- 「今っぽい」の具体的なデザイン基準は未確認。
- 現在のデザインは、実装者判断によるコンパクト・角丸・グラデーション表現。
  - 採用理由は要望に対する実装判断であり、詳細なデザイン合意は未確認。

### 関連制約

- 過剰な装飾を避け、保守しやすい CSS に留める。

## 2026-06-02 / タスク追加・編集パネル

### 決定事項

- タスク追加・編集は別ページ遷移ではなく、パネルで行う。
- 入力項目は以下にする。
  - 種類: 買い物 / やる事
  - タスク名
  - ステータス: 未完了 / 完了

### 理由

- 初期要件で、別画面に遷移せず右側パネルまたはモーダルで表示する案が示されたため。
- 入力項目が明示されていたため。

### 却下案

- 追加・編集専用ページへの遷移は採用しない。
  - 理由: 別画面に遷移しない要件があったため。
- 期限・メモ・優先度などの追加フィールドは採用しない。
  - 理由: 入力項目として明示されておらず、推測補完になるため。

### 注意点

- 種類は現状 `shopping` / `todo` を内部値とし、表示は「買い物」/「やる事」。
- タスク名は空白のみの場合は保存しない。

### 関連制約

- 推測で項目を増やさない。
- 過剰設計を避ける。

## 2026-06-02 / 設定画面

### 決定事項

- 設定画面には以下を置く。
  - データのエクスポート JSON
  - データのインポート JSON
  - 全データ削除
  - テーマ切替: ライト / ダーク
  - ストレージ使用状況

### 理由

- 初期要件で設定画面の項目として明示されたため。
- localStorage 運用では、JSON export/import がバックアップ・復元手段になるため。

### 却下案

- サーバー同期・クラウドバックアップは採用しない。
  - 理由: 要件外であり、localStorage 方針と異なるため。

### 注意点

- JSON import は `Task` 形状に近い配列のみ受け入れる。
- 全データ削除は確認ダイアログを出す。
- ストレージ使用状況は対象キーの概算サイズ表示。

### 関連制約

- localStorage 保存であること。
- 高度なバックアップ機能は過剰設計。

## 2026-06-02 / ドキュメント・運用

### 決定事項

- README は日本語で、機能・データ構造・起動方法・ビルド方法を記載する。
- 今後の AI 継続利用用に、この `decisions.md` を追加する。
- `decisions.md` は単なる会話要約ではなく、設計判断の理由を残すための記録にする。

### 理由

- ユーザーから `decisions.md` 生成の明確な依頼があったため。
- 今後の会話で同じ議論を繰り返さず、なぜその仕様になったかを保持するため。

### 却下案

- 会話ログの単純要約にする案は採用しない。
  - 理由: ユーザーが「将来の設計判断に必要な記録」として整理することを求めたため。
- 未決事項や仮案を含める案は採用しない。
  - 理由: 決定済みのみ記載するルールがあるため。

### 注意点

- 推測で補完しない。
- 理由が弱いものは「理由不足」と明記する。
- 未確認事項は未確認と書く。

### 関連制約

- ユーザー指定の出力形式: Markdown。
- ユーザー指定の構成: 決定事項 / 理由 / 却下案 / 注意点 / 関連制約。
