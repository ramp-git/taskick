{
  "name": "taskick",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 5173",
    "build": "rm -rf dist && mkdir -p dist/src && cp index.html dist/index.html && cp src/main.js src/styles.css dist/src/"
  }
}
